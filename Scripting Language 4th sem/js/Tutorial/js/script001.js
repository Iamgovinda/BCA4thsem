console.log(document)

a = document.images;

Array.from(a).forEach(function(element)
{
    console.log(element);
})

console.log(document.body.classList)
console.log(document.links[0].href)

//so with the help of document object we can manipulate all the elements and their attributes easily!

